"""O(1) - Constant Time
O(1) describes an algorithm that will always execute in the
same time (or space) regardless of the size of the input data set.
"""


def is_first_element_none(elements):
    """Return True if first element is None, otherwise return false"""
    return elements[0] is None
